#include<stdio.h>
#include<math.h>
int main(){
    int b,c;
    b = c = 2;
    int a = b + c;
    int power = pow(b,c);
    printf("%d", power);
    return 0;
    // modulo operator
    // it gives remainder in return;
    // like 5%2 = 1;
    // but in some case it will 2%5 = 2;
    // won't work for float values
    
}