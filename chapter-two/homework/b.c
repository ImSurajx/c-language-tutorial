// wap to check if given character is digit or not;
#include<stdio.h>
int main(){
    // i will back in future. now it's hard.
    int digit;
    printf("enter any thing: ");
    scanf("%d",&digit);
    printf("%d", digit+digit || digit-digit || digit*digit || digit/digit || digit%digit);
    return 0;
}