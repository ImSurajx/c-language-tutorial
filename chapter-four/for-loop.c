#include<stdio.h>
int main(){
    // iterator  ; counter
    // we can also use any data type as a counter
    for (float i = 100; i >= 1.0; i--)
    {
        printf("%f \n",i);
    }
    
    return 0;
}