#include<stdio.h>
int main(){
    // wap to find area of circle pie * radius ^ 2
    float radius;
    printf("enter radius of circle: ");
    scanf("%f", &radius);
    printf("area of circle is: %f unit ^ 2", 3.14 * radius * radius );
    return 0;
}