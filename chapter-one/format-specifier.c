#include<stdio.h>
int main(){
    /*
    for output.
    %d, %f, %c, this things called as format specifier in different data type we have different format specifier.
    1. integer
    printf("age is %d", age);

    2. real numbers
    printf("values of pi is %f", pi);

    3. characters
    printf("star looks like this %c", star);
    */

   /*
   for input
   scanf("%d", &age);
   */
    return 0;
}
