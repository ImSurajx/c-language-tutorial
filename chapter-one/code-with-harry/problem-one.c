/* write a c program to calculate are of rectangle 
    a. using hard coded inputs
*/
#include<stdio.h>
int main(){
    int length = 10, bredth = 8;
    int area = length * bredth;
    printf("the area of rectangle is: %d unit ^ 2\n", area);
    return 0;
}